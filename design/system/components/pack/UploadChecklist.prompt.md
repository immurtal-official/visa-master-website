Upload checklist where every item states why it is needed.

```jsx
<UploadChecklist items={items} onAction={open} />
```

States: done / checking / todo / redo / optional. `redo` uses amber, never red.