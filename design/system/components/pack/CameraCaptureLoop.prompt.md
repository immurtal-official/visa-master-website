Shoot-keep-shoot loop for multi-page documents, with reorderable thumbnails.

```jsx
<CameraCaptureLoop pages={pages} onCapture={shoot} onRemove={rm} onMove={move} />
```

Page order matters to the consulate, so the reorder controls are always visible — not behind a long-press only.