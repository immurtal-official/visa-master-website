Header for both the marketing site and the app; `Wordmark` is exported alongside.

```jsx
<SiteHeader nav={[{label:"支持路线"},{label:"价格"}]} action={<Button size="sm">开始准备材料</Button>} />
```

Pass `compact` below 900px. There is no supplied logo file — the wordmark is set in the system stack.