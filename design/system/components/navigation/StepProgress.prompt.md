Progress affordance for the intake: section name, step count, bar, section dots.

```jsx
<StepProgress section="行程与费用" step={12} total={34} sections={[{name:"基本信息",state:"done"}]} />
```

Sits directly under the header on every intake page. Never hide it behind a menu.