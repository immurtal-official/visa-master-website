One per view. Survives the on-screen keyboard and respects the home-indicator inset.

```jsx
<StickyActionBar note="已自动保存" secondary={<Button variant="ghost">保存并退出</Button>}>
  <Button variant="primary" size="lg" block>继续</Button>
</StickyActionBar>
```