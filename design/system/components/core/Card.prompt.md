White panel on the blue-grey page — the default container for everything.

```jsx
<Card header="材料包内容" footer={<Button variant="secondary">全部下载</Button>}>…</Card>
```

Border does the separating; elevation 2 is reserved for things that genuinely float. `tone="inverse"` for navy sections.