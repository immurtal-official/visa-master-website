Masked Lucide glyph that inherits currentColor — the only icon primitive in the system.

```jsx
<Icon name="circle-check" size={18} />
```

Use 16px inline in text, 18px inside buttons, 20-24px standalone. Pass `title` only when the glyph carries meaning no nearby text carries; otherwise it stays decorative and hidden from screen readers.