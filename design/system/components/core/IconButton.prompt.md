Icon-only 44x44 control for toolbars and thumbnail actions.

```jsx
<IconButton icon="download" label="下载这一份" variant="solid" />
```

`label` is mandatory. Use `solid` when it sits on a photo or a busy panel.